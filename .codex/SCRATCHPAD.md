# abc-website scratchpad

- Repository starts intentionally minimal.
- `logo-generator/` is a Git submodule pointing at `../abc-logo-generator`.
- Hold off on adding a site framework until that decision is made explicitly.
