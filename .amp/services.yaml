services:
  web:
    command: corepack pnpm exec astro dev --host 0.0.0.0 --port "$PORT"
    health: /
    portal:
      title: ABC PR preview
      description: Review the pull request's affected pages before approving it.
